[package]
name = "interpreter"
version = "0.1.0"
edition = "2021"

[lib]
name = "interpreter"
path = "src/lib.rs"

[[bin]]
name = "interpreter"
path = "src/main.rs"

[dependencies]
clap = { version = "4.3.2", features = ["derive"] }
